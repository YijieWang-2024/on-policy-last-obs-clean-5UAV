# PASSED_WITH_WARNINGS

The final-trace figures use every saved TensorBoard event for each run. They do not claim that different run endpoints are a fair matched-step comparison; use the original common-step summaries for that purpose. MovingHotspot seed 2 remains an interrupted run.
