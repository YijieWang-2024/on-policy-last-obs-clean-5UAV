# 5-UAV Paper Reproduction Workspace

> 2026-07-28 状态：本文件保留仓库来源与旧论文图复现约束。当前动态用户实验的最新进展、正负结果和运行状态请以 [`RECENT_EXPERIMENT_SUMMARY_20260725_20260728.md`](RECENT_EXPERIMENT_SUMMARY_20260725_20260728.md) 为准。当前工作分支是 `agent/dcppo-runtime-optimization`；下方 `paper-5uav-repro` 是该工作区建立时的历史身份，不代表当前 checkout。

## Identity

- Git branch: `paper-5uav-repro`
- Base commit: `fcb891d` (2025-09-30 16:29:27 +0800)
- Base commit message: `5架无人机最终可以训练的代码`
- Purpose: main paper experiments, including the default 5-UAV/60-MD setting and scalability comparisons.

The historical directory `on-policy-separated-last-obs` remains unchanged. This workspace is a clean worktree created from the final 5-UAV commit, with the minimum plotting scripts and processed data copied in for paper-figure traceability.

## Code Documentation

See `CODE_WALKTHROUGH_5UAV.md` for the Chinese walkthrough of the MEC environment, action projection, neighborhood-attention critic, advantage aggregation, final `run30901` configuration, and the paper/code consistency audit.

## Paper Figure Map

| Paper figure | Script/data in this workspace | Status |
| --- | --- | --- |
| Fig. 3(c) trajectory | `onpolicy/plot_UAV_trajectory.py`, `onpolicy/scripts/train/-0.8320*uav/gu*_all_time.npy` | Script contains explicit coordinate post-processing; preserve for provenance and review before reuse. |
| Fig. 3(d) association | `onpolicy/plot_offloading_association.py`, `-0.8320*` arrays | Uses the same post-processed trajectory coordinates. |
| Fig. 4 training curves | `onpolicy/tensorboard_plot_var-NotLook.py`, `onpolicy/scripts/train/plot_data/*.npy` | `ara_extended.npy` was generated by `extend_ara_curve.py`; it is not a complete raw ARA training trace. |
| Fig. 5 UAV scalability | `onpolicy/plot_UAVs.py`, `plot_data/system_gain_*.txt` | Processed seed summaries are included. |
| Fig. 6 MD scalability | `onpolicy/plot_GUs.py`, `plot_data/system_gain_*.txt` | Processed seed summaries are included. |
| Fig. 7 k-hop comparison | `onpolicy/tensorboard_plot_var-NotLook.py`; historical helpers in `onpolicy/legacy_analysis/` | Published image is retained for comparison; original TensorBoard logs remain in the historical directory. |
| Fig. 8 communication payload | `onpolicy/plot_uav_flood_vs_consensus.py`, `plot_flood_consensus_data/` | Published image and synthetic-input provenance are included. |

Canonical submitted images are under `paper_artifacts/published_figures/`.

## Data Policy

Small processed arrays and summaries required to regenerate paper figures are intentionally kept with the code. Heavy checkpoints, TensorBoard event files, generated images, and `scripts/results/` are excluded by `.gitignore` and remain in the historical directory.

The `onpolicy/legacy_analysis/` directory contains exploratory utilities preserved for provenance. It is not part of the main training entry point.

## Important Integrity Notes

1. `extend_ara_curve.py` extends an incomplete ARA curve using a stochastic AR(1) process and a configured final target. Treat the resulting curve as historical post-processing, not as independently observed training episodes.
2. The trajectory and association scripts alter loaded UAV coordinates before plotting. Do not describe those coordinates as raw simulator output without disclosure or regeneration.
3. Run plotting scripts from the `onpolicy` directory because several historical scripts use paths relative to that directory.
4. The saved Fig. 4 arrays (`mappo.npy`, `dcppo.npy`, and `ara_extended.npy`) have shape `(3, 1750)`, so they contain three curves. This does not match the manuscript statement that all results are averaged over five random seeds and must be resolved before making a reproducibility claim.
