# DC-PPO 通信半径结果导向调参记录（2026-08-02）

## 目标与诚实边界

目标是在完全匹配的训练协议下检验并尽可能获得：

```text
DC-PPO-R0 < DC-PPO-R260 < DC-PPO-R1000
```

该顺序必须在预先固定的多个随机种子和固定起点评估中成立，不能通过为不同通信半径分别选择超参数、删选种子或人为修改曲线来制造。

## 已经确定的事实

1. 通信实现本身有效。当前主实验中，R0、R260、R1000 的通信图分别接近 self-only、部分连通、完全连通；共识到全局均值的误差随半径严格下降。
2. 性能没有随通信质量单调改善。2026-08-02 约 22M 环境步的 `mixed_consensus(alpha=0.5)` 最近 25 点均值为：R0 `536.9k`、R260 `521.8k`、R1000 `510.3k`。
3. 单独改变 critic 信息范围几乎没有收益。远程 `local` 对照在约 37M 时为：R260 `555.3k`、R1000 `551.6k`，差异小于 1%。
4. 纯共识优势破坏信用分配。约 37M 时，Pure R260 `448.3k`、Pure R1000 `376.3k`，显著低于 Local，并且信息更多的 R1000 更差。
5. 共识轮次不是当前主矛盾。R520 的 K=1/5/50 在约 33--34M 时分别为 `504.7k/517.8k/516.1k`，没有单调关系。
6. mean-pool critic 明显弱于 ego-query critic，但修正 critic 结构仍未产生半径单调收益。

证据目录：

- `paper_artifacts/diagnostics/mixed_r1000_r260_r0_live_20260802_2218/`
- `paper_artifacts/diagnostics/ego_vs_meanpool_true60_20260802/`
- `paper_artifacts/diagnostics/q2_dcppo_ippo_live_20260801_1715/`
- `paper_artifacts/diagnostics/r1000_r260_r0_live_20260802_1500/`

## 当前判断

“critic 输入更多信息”只意味着理想最优 critic 的条件价值估计误差不应变差；它不保证有限网络、有限样本和 PPO 更新下的最终策略回报单调提高。critic 是 baseline，不会扩大 actor 执行时能看到的信息，也不会自动提供更准确的跨 UAV 信用分配。

当前环境中局部优势已经很强：资源由各 UAV 独立分配，MD 在十个时隙内只移动约 2.5m，未准入 MD 立即消失。加入高度相关的团队优势主要改变梯度方差和信用归属，收益信号却很少，因此 R0/Local 容易胜出。

## 第一候选：只加入通信带来的外部性增量

当前 component-scaled consensus 的收敛目标是：

```text
G_i^R = sum_{j in component(i,R)} A_j / M
```

新模式定义：

```text
E_i^R = M * G_i^R - c_i^K * A_i
U_i(beta) = A_i + beta * E_i^R
```

随后只执行 PPO 原本就有的一次逐 UAV 优势标准化。

`c_i^K=|C_i|[W^K]_{ii}` 是有限轮共识中自身优势的真实系数；显式减去它可避免把尚未收敛的自身残留误称为“其他 UAV 信息”。共识收敛后 `c_i^K=1`。

- R0：component 只有自己，`E_i=0`，标准化后严格退化为局部 PPO。
- R260：只加入同一通信分量内其他 UAV 的增量信息。
- R1000：加入其余四架 UAV 的完整增量信息；`beta=1` 时得到全局优势之和，经 PPO 标准化后与全局均值完全等价。
- 保留 `A_i` 解决局部信用分配；不再像 mixed consensus 那样重复加入自己的优势。
- 不单独标准化 `E_i`，避免把微弱通信残差强制放大为单位方差，也避免孤立样本被减均值后变成伪外部性。

该模式名为 `externality_consensus`，参数为 `--externality_beta`。旧的 `local/mixed_consensus/pure_consensus/legacy_noise` 均不改动。

## 筛选矩阵与门槛

### Stage 1：R1000 单种子筛选

固定其余参数，使用 seed=2、64 并行环境、K=50、100M 上限。第一批只占用三个释放出来的槽：

```text
beta = 0.025, 0.05, 0.10
```

预先规定：如果 `0.10` 最优且仍明显上升，再补 `0.20/0.40`；如果 `0.025` 最优，再补 `0.01`；若内部点 `0.05` 最优，则不扩网格。这样避免看曲线后任意挑点。

门槛：

- 20M 只用于灾难性停止：NaN、训练崩溃、True60 比匹配 Local 低 10% 以上。
- 50M 才做筛选结论，因为 reset curriculum 到 50M 才完全切换为固定起点。
- 保留 1--2 个不低于 Local R1000、完成率未恶化且外部性诊断稳定的 beta。

### Stage 2：三半径配对筛选

对 Stage 1 胜出的 beta，使用相同 seed 同时跑 R0/R260/R1000。R0 只需一个，因为其结果与 beta 无关。

筛选标准：

- 50M 后固定起点阶段的最后 5M/10M 均值；
- 0--50M AUC 仅作为样本效率指标；
- `system_performance_true_all_GUs`、任务完成率、接纳率同时检查；
- 统一 checkpoint 做固定起点测试，不在训练中开启额外 eval worker。

### Stage 3：独立种子确认

冻结 beta 后，至少使用 3 个未参与选择的种子进行 100M 三半径验证。目标门槛预先固定为：

- 三种子均值满足 R1000 > R260 > R0；
- `R1000-R260` 和 `R260-R0` 均至少约 1%；
- 至少 2/3 个种子保持相同顺序；
- 同时报告标准差和失败种子，不做选择性删除。

## 当前实验的停止/保留建议

- 保留到 50M：本地 Mixed R0/R260/R1000（三半径基线）；远程 Local R260/R1000（critic 信息范围对照）。
- 可以停止并释放资源：Pure R260/R1000；纯团队优势相对 Local 已落后约 19%--32%，机制诊断已经充分。
- Mean-pool R1000 保留到 50M，作为 critic 聚合结构对照。
- K=1/5/50 不支持“轮次越多越好”；为保留最有信息量的端点，K1/K50 跑到 50M，K5 与 K50 只差约 0.3%，可先停止释放第三个槽。后续主线先统一 K=50 保持论文定义。

## 若第一候选仍失败

按顺序推进，不同时堆叠变量：

1. 双头 critic：本地 head 学 `R_i`，团队/外部性 head 学其他 UAV 增量回报，actor 仍使用 `z(A_i)+beta*z(E_i)`。
2. 让 actor 获得任务相关通信消息，而不是只让 critic 多看状态：邻居相对位置、负载、活跃用户数、覆盖质心和剩余资源。
3. 最后才增强环境耦合（例如提高 MD 速度或加入切换/共享资源耦合）。这会改变系统场景，必须作为新环境实验单独报告。

不优先采用“按半径人为增大随机噪声”的路线。它最容易制造目标顺序，但除非论文明确建模通信误差分布，否则难以成为可信的算法证据。
