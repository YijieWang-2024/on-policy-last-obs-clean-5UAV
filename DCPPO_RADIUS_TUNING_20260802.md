# DC-PPO 通信半径结果导向调参记录（2026-08-02）

## 目标与诚实边界

目标是在完全匹配的训练协议下检验并尽可能获得：

```text
DC-PPO-R0 < DC-PPO-R260 < DC-PPO-R1000
```

该顺序必须在预先固定的多个随机种子和固定起点评估中成立，不能通过为不同通信半径分别选择超参数、删选种子或人为修改曲线来制造。

## 已经确定的事实

1. 通信实现本身有效。当前主实验中，R0、R260、R1000 的通信图分别接近 self-only、部分连通、完全连通；共识到全局均值的误差随半径严格下降。
2. 性能没有随通信质量单调改善。2026-08-02 约 22M 环境步的 `mixed_consensus(alpha=0.5)` 最近 25 点均值为：R0 `536.9k`、R260 `521.8k`、R1000 `510.3k`。
3. 单独改变 critic 信息范围几乎没有收益。远程 `local` 对照在约 37M 时为：R260 `555.3k`、R1000 `551.6k`，差异小于 1%。
4. 纯共识优势破坏信用分配。约 37M 时，Pure R260 `448.3k`、Pure R1000 `376.3k`，显著低于 Local，并且信息更多的 R1000 更差。
5. 共识轮次不是当前主矛盾。R520 的 K=1/5/50 在约 33--34M 时分别为 `504.7k/517.8k/516.1k`，没有单调关系。
6. mean-pool critic 明显弱于 ego-query critic，但修正 critic 结构仍未产生半径单调收益。

证据目录：

- `paper_artifacts/diagnostics/mixed_r1000_r260_r0_live_20260802_2218/`
- `paper_artifacts/diagnostics/ego_vs_meanpool_true60_20260802/`
- `paper_artifacts/diagnostics/q2_dcppo_ippo_live_20260801_1715/`
- `paper_artifacts/diagnostics/r1000_r260_r0_live_20260802_1500/`

## 当前判断

“critic 输入更多信息”只意味着理想最优 critic 的条件价值估计误差不应变差；它不保证有限网络、有限样本和 PPO 更新下的最终策略回报单调提高。critic 是 baseline，不会扩大 actor 执行时能看到的信息，也不会自动提供更准确的跨 UAV 信用分配。

当前环境中局部优势已经很强：资源由各 UAV 独立分配，MD 在十个时隙内只移动约 2.5m，未准入 MD 立即消失。加入高度相关的团队优势主要改变梯度方差和信用归属，收益信号却很少，因此 R0/Local 容易胜出。

## 第一候选：只加入通信带来的外部性增量

当前 component-scaled consensus 的收敛目标是：

```text
G_i^R = sum_{j in component(i,R)} A_j / M
```

新模式定义：

```text
E_i^R = M * G_i^R - c_i^K * A_i
U_i(beta) = A_i + beta * E_i^R
```

随后只执行 PPO 原本就有的一次逐 UAV 优势标准化。

`c_i^K=|C_i|[W^K]_{ii}` 是有限轮共识中自身优势的真实系数；显式减去它可避免把尚未收敛的自身残留误称为“其他 UAV 信息”。共识收敛后 `c_i^K=1`。

- R0：component 只有自己，`E_i=0`，标准化后严格退化为局部 PPO。
- R260：只加入同一通信分量内其他 UAV 的增量信息。
- R1000：加入其余四架 UAV 的完整增量信息；`beta=1` 时得到全局优势之和，经 PPO 标准化后与全局均值完全等价。
- 保留 `A_i` 解决局部信用分配；不再像 mixed consensus 那样重复加入自己的优势。
- 不单独标准化 `E_i`，避免把微弱通信残差强制放大为单位方差，也避免孤立样本被减均值后变成伪外部性。

该模式名为 `externality_consensus`，参数为 `--externality_beta`。旧的 `local/mixed_consensus/pure_consensus/legacy_noise` 均不改动。

## 筛选矩阵与门槛

### Stage 1：R1000 单种子筛选

固定其余参数，使用 seed=2、64 并行环境、K=50、100M 上限。第一批只占用三个释放出来的槽：

```text
beta = 0.025, 0.05, 0.10
```

预先规定：如果 `0.10` 最优且仍明显上升，再补 `0.20/0.40`；如果 `0.025` 最优，再补 `0.01`；若内部点 `0.05` 最优，则不扩网格。这样避免看曲线后任意挑点。

门槛：

- 20M 只用于灾难性停止：NaN、训练崩溃、True60 比匹配 Local 低 10% 以上。
- 50M 才做筛选结论，因为 reset curriculum 到 50M 才完全切换为固定起点。
- 保留 1--2 个不低于 Local R1000、完成率未恶化且外部性诊断稳定的 beta。

### Stage 2：三半径配对筛选

对 Stage 1 胜出的 beta，使用相同 seed 同时跑 R0/R260/R1000。R0 只需一个，因为其结果与 beta 无关。

筛选标准：

- 50M 后固定起点阶段的最后 5M/10M 均值；
- 0--50M AUC 仅作为样本效率指标；
- `system_performance_true_all_GUs`、任务完成率、接纳率同时检查；
- 统一 checkpoint 做固定起点测试，不在训练中开启额外 eval worker。

固定起点评估使用两组互不重叠的显式种子：

- 开发/筛选集：`61001--61020`，只用于选择 beta；
- 最终保留验证集：`71001--71040`，beta 与全部超参数冻结后才首次使用。

两组都采用确定性 actor、保存的冻结观测归一化统计以及关闭 curriculum 的固定 UAV 起点。训练过程不启动 eval worker，因此评估不会降低训练 FPS。

固定评估的审计约束如下：

- 评估前必须暂停/结束训练，使 `models/` 不再被写入；五套独立 actor 和 normer 是顺序保存的，禁止从正在覆盖的目录抓正式快照；
- 新提交会在五套 actor/critic/normer 全部保存完成后，以原子替换方式发布 `checkpoint_manifest.json`，记录 session/source/cumulative 训练步数、累计步数来源、每个文件的大小与 SHA-256，并绑定对应的 `args.json`；评估和续训在复制/加载前后都会验证清单与十五个文件。使用该提交续训时会从源清单继承累计步数；旧模型没有清单时必须显式提供 `--checkpoint_base_steps` 和 `--confirm_legacy_checkpoint_frozen`，其累计历史会标为人工声明而非完全验证。当前正在运行的九路 Actor 消息实验由旧提交启动，没有该清单，属于 legacy checkpoint：其 50M 正式快照必须等到第一个不小于 50M 的保存点 `50,073,600` 环境步，先暂停训练并确认文件不再变化，再复制和恢复训练；固定评估或渲染均须传入 `--confirm-checkpoint-frozen`，且不能事后仅凭日志中的最新步数替模型补写清单。
- 命令必须显式给出相同的训练步数，并使用 `--confirm-checkpoint-frozen`；比较工具拒绝步数不同、缺种子、重复种子及任何非有限指标；
- 除预先允许变化的通信半径、actor 消息模式、优势模式、beta 与运行元数据外，比较工具对完整训练/环境参数生成 SHA-256 指纹并要求完全相同，避免误把不同 MD 速度、生命周期、curriculum 或 PPO 配置混入同一对比；
- 主要指标预先固定为 `system_performance_true_all_GUs`；每种方法报告均值、样本标准差与 95% t 区间，算法差异报告同 seed 的配对差、95% 配对 t 区间和 sign-flip 稳健性检验；
- 延迟、能耗、轨迹长度和稳定时隙均为越低越好，比较结果同时保留原始 `candidate-baseline` 与按方向修正后的 improvement；
- `final_position_stability_slot_10m` 只表示“从该时隙起始终位于最终位置 10m 内”，不是“已经达到全局最优部署”；同时记录最后 50 时隙轨迹长度以诊断到达后是否继续晃动；
- 相同 seed 保证相同初始随机状态，但当前动态 MD 的后续随机数消耗会受策略接纳行为影响，因此不是逐时隙完全相同的用户轨迹。配对结果是固定 checkpoint 的同 seed 比较，不能替代多个训练 seed；
- `61001--61020` 一旦用于选 beta 就只属于开发集；查看 `71001--71040` 后若再次修改规则，必须更换最终验证种子。
- sign-flip p 值只作为稳健性诊断，不作为多指标挑选依据；最终主结论只看预先定义的 True60 相邻差 `R260-R0` 和 `R1000-R260` 及其区间，其他指标均为次要或诊断指标。

### Stage 3：独立种子确认

冻结 beta 后，至少使用 3 个未参与选择的种子进行 100M 三半径验证。目标门槛预先固定为：

- 三种子均值满足 R1000 > R260 > R0；
- `R1000-R260` 和 `R260-R0` 均至少约 1%；
- 至少 2/3 个种子保持相同顺序；
- 同时报告标准差和失败种子，不做选择性删除。

## 当前实验的停止/保留建议

- 保留到 50M：本地 Mixed R0/R260/R1000（三半径基线）；远程 Local R260/R1000（critic 信息范围对照）。
- 可以停止并释放资源：Pure R260/R1000；纯团队优势相对 Local 已落后约 19%--32%，机制诊断已经充分。
- Mean-pool R1000 保留到 50M，作为 critic 聚合结构对照。
- K=1/5/50 不支持“轮次越多越好”；为保留最有信息量的端点，K1/K50 跑到 50M，K5 与 K50 只差约 0.3%，可先停止释放第三个槽。后续主线先统一 K=50 保持论文定义。

## 若第一候选仍失败

按顺序推进，不同时堆叠变量：

1. 双头 critic：本地 head 学 `R_i`，团队/外部性 head 学其他 UAV 增量回报，actor 仍使用 `z(A_i)+beta*z(E_i)`。
2. 让 actor 获得任务相关通信消息，而不是只让 critic 多看状态：邻居相对位置、负载、活跃用户数、覆盖质心和剩余资源。
3. 最后才增强环境耦合（例如提高 MD 速度或加入切换/共享资源耦合）。这会改变系统场景，必须作为新环境实验单独报告。

不优先采用“按半径人为增大随机噪声”的路线。它最容易制造目标顺序，但除非论文明确建模通信误差分布，否则难以成为可信的算法证据。

## 2026-08-03：Actor 任务消息主线

### 动机与边界

此前仅改变 critic 可见范围时，R260/R1000 的性能差通常很小；这并不意外，因为 critic 信息只改变 baseline 质量，执行时的局部 actor 并没有因此知道远端需求方向。新主线让 actor 的飞行动作也接收通信半径内的 sender-local 任务摘要，同时保留半径相关的 attention critic。

第一轮统一使用纯局部优势 `A_i`，不叠加 `A_mean`、externality 或人工噪声。这样首先回答“执行时的真实任务消息是否有用”，避免把消息效果与优势融合重新混在一起。消息首轮只进入 Spatial Flight head，不进入关联、带宽和计算资源 head，因此本轮结论只针对轨迹和部署决策。

### 固定消息协议

每个可通信邻居发送 10 维包：

```text
[relative_x, relative_y,
 active_user_count,
 covered_user_centroid_x, covered_user_centroid_y,
 mean_user_velocity_x, mean_user_velocity_y,
 user_spread, hard_task_fraction,
 presence_mask]
```

- `relative_x/y` 按固定地图尺度归一化；用户质心和离散度按固定覆盖半径归一化；用户速度按固定 UAV 最大速度归一化。所有缩放常数不随通信半径或 MD 平均速度变化。
- sender 的完整覆盖用户数本来就是其局部观测字段；其余详细统计只从 sender 自己实际保留的前 `K_md=20` 条用户记录计算，不读取未来结果、其他连通分量或全局用户真值。
- `hard_task_fraction` 使用动作前的本地计算可完成性判据，与现有二级用户排序一致，不使用本时隙动作或完成结果。
- `zero` 模式连 mask 一起清零；`geometry` 只保留相对位置和 mask；`task_summary` 在 geometry 上增加任务统计。首轮只比较 zero 与完整 task summary，确认有效后才补 geometry 消融。
- 重叠覆盖中的同一 MD 可能由多个 sender 各自汇报，这是合法的去中心化重复观测，不解释为去重后的全局用户统计。

### 首批六路实验

全部使用 seed=2、64 个并行环境、100M 上限、相同 reset curriculum、Spatial Flight Actor、二级完成性排序、K=50 和纯 `A_i`：

| 实验 | 半径 | 消息 | MD均速 | 目的 |
|---|---:|---|---:|---|
| A0 | 0 | task_summary（实际全零） | 0.5 | 同架构 self-only 锚点 |
| A1 | 260 | task_summary | 0.5 | 低速主排序中点 |
| A2 | 1000 | task_summary | 0.5 | 低速主排序上界 |
| A3 | 1000 | zero | 0.5 | 与 A2 同半径隔离 Actor 消息增益 |
| V0 | 0 | task_summary（实际全零） | 10 | 高机动 self-only 锚点 |
| V1 | 1000 | task_summary | 10 | 检查增强移动耦合后通信是否更有价值 |

0.5 m/s 的 A0/A1/A2 直接检验目标排序；A2-A3 判断 R1000 的收益是否真的来自 Actor 消息，而不只是 critic 半径。10 m/s 改变了环境定义，只比较 V1-V0，不把其 True60 绝对值与低速场景混合。若高速端点显示明确收益，再补 R260；若没有收益，不继续扩大高速网格。

### 冻结判据

- 20M 只做灾难性停止：NaN/Inf、消息 mask/数值错误，或相对同速度匹配控制落后 10% 以上。
- 50M 冻结 checkpoint 后，在开发种子 `61001--61020` 做固定起点确定性评估；训练期间不开额外 eval worker，避免降低 FPS。
- 主指标为 True60；同时要求完成率和接纳率不能各下降超过 1 个百分点。
- Actor 消息的主效应定义为 `A2-A3`。固定评估提升达到 1% 即视为有实际筛选价值；0--1% 的正差只记为弱信号，必须同时满足配对区间没有明显负向、完成率/接纳率不恶化，才允许继续到 100M。若差值不为正且测试时屏蔽消息几乎不改变飞行动作，则认为当前 packet 没有被有效利用，不通过临时调整优势噪声制造排序。
- 对 message-capable checkpoint 额外运行同种子的 `--mask-actor-messages` 反事实评估：仅把 actor 消息 payload 与 presence mask 置零，环境、critic状态、checkpoint和冻结归一化统计均不变。正常评估与屏蔽评估的配对差用于确认策略是否实际利用通信消息；该反事实诊断不能代替不同训练配置间的主要比较。
- 单 seed 只作机制筛选；只有冻结协议后至少 3 个训练 seed 的均值满足 R1000 > R260 > R0，且至少 2/3 seed 保持该顺序，才形成论文级结论。

### GPU2/3/4释放后的三条桥接实验

停止 externality beta 扫参后，用释放的三个 GPU 启动以下单因素桥接实验。其余训练配置与 A/V 系列完全一致：

| 实验 | 半径 | 消息 | MD均速 | 直接对照与问题 |
|---|---:|---|---:|---|
| B1 | 260 | zero | 0.5 | 与 A1 比较，隔离 R260 下 task message 的真实增益 |
| B2 | 1000 | geometry | 0.5 | 与 A3/A2 比较，把 R1000 消息收益分解为几何信息与任务摘要 |
| B3 | 260 | task_summary | 10 | 补齐 V0/B3/V1 的高速 R0/R260/R1000 半径序列 |

这三条不改变优势函数，仍然统一使用纯 `A_i`。因此九路能够回答三组互不混淆的问题：

1. 低速目标排序：A0/A1/A2；
2. Actor消息因果增益：A1-B1，以及 A2-A3；
3. 消息内容消融：A3-B2-A2；
4. 高速目标排序：V0/B3/V1。

若 task summary 不优于同半径 zero，而 geometry 优于 zero，则说明完整任务包噪声过多，应保留更简单的邻居几何消息；若 task 优于 zero 但 R1000 仍不优于 R260，则优先检查远端无关消息或重叠热点重复加权，而不是继续调整优势噪声。

### 2026-08-03 02:33 只读进度快照

本节只记录当前证据，不用于提前选择配置。

本地三路 Mixed-consensus 已运行到约 31--32M，FPS 为 850--858。按共同 TensorBoard step 计算的最近 25 点结果为：

| 对比 | 全部 reset True60 | fixed-reset True60 |
|---|---:|---:|
| R260 − R0 | −3.13% | −3.12% |
| R1000 − R260 | −2.22% | −2.23% |

当前排序仍与目标相反，而且 fixed/random 条件下方向一致；但 20--50M 仍处于 reset 概率从 0.5 退火到 0 的阶段，尚未达到预注册的 50M 固定起点评估点。这两条负差均未达到 10% 灾难性停止阈值，因此三路继续运行到 50M。

远程九路 Actor 消息实验当前约 2.2--4.0M，FPS 为 992--1229，未发现 NaN、OOM 或 Traceback。按每一对能够共同获得的最后 25 个事件点计算：

| 对比 | 共同终点 | True60差异 |
|---|---:|---:|
| A1 − A0：低速 R260 − R0 | 3.968M | −3.41% |
| A2 − A1：低速 R1000 − R260 | 3.968M | −0.64% |
| A1 − B1：R260 task − zero | 2.534M | −4.29% |
| A2 − A3：R1000 task − zero | 3.917M | +6.89% |
| B2 − A3：R1000 geometry − zero | 2.381M | −0.57% |
| A2 − B2：R1000 task − geometry | 2.381M | +1.68% |
| B3 − V0：高速 R260 − R0 | 2.227M | −2.41% |
| V1 − B3：高速 R1000 − R260 | 2.227M | −4.41% |

R1000 task 相对 zero 暂时出现正信号，但其他相邻半径仍为反序，而且桥接实验启动更晚。该阶段的差异容易随训练迅速改变，20M 前只用于检查实现与灾难性退化，不据此停止、扩网格或宣称消息有效。小时级只读心跳现统一覆盖远程九路和本地 Mixed R0/R260/R1000 三路，不再引用已经过期的旧 PID。
