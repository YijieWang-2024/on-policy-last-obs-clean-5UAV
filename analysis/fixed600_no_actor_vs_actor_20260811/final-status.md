PASSED_WITH_WARNINGS

The two requested figures and the rerunnable plotting script were generated from
the refreshed remote snapshots and the latest local event file.  The remote
runs are still in progress; the local actor-message run is stopped before
60M steps, so the four-curve figure is a current-progress comparison rather
than a matched-final-budget comparison.
